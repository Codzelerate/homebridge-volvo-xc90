{
  "pluginAlias": "VolvoXC90",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Control your Volvo XC90 2016 from Apple Home via the Volvo Connected Vehicle API v2. Get your VCC API Key at [developer.volvocars.com](https://developer.volvocars.com).\n\n**First-time setup:** Fill in all fields, save, and restart. Volvo will email you a 6-digit OTP — paste it into the OTP field, save, and restart again. Once authenticated, you can remove your email, password, and OTP from the config. Only the VCC API Key and VIN are needed going forward.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform name",
        "type": "string",
        "default": "Volvo XC90"
      },
      "vccApiKey": {
        "title": "VCC API Key",
        "type": "string",
        "required": true,
        "description": "Primary API key from your application on developer.volvocars.com. Always required."
      },
      "vin": {
        "title": "VIN",
        "type": "string",
        "minLength": 17,
        "maxLength": 17,
        "required": true,
        "description": "17-character Vehicle Identification Number. Always required."
      },
      "username": {
        "title": "Volvo ID (email)",
        "type": "string",
        "format": "email",
        "description": "Only needed for first-time setup or if the refresh token expires. Can be removed after successful login."
      },
      "password": {
        "title": "Volvo ID password",
        "type": "string",
        "description": "Only needed for first-time setup or if the refresh token expires. Can be removed after successful login."
      },
      "otp": {
        "title": "One-Time Password (OTP)",
        "type": "string",
        "description": "6-digit code from the Volvo email. Only needed during initial setup. Remove after successful login."
      },
      "forceReauth": {
        "title": "Force re-authentication",
        "type": "boolean",
        "default": false,
        "description": "Clears the stored session on next restart, triggering a fresh OTP login. Enable this + add your credentials, save and restart — you will receive a new OTP email. Disable again once authenticated."
      },
      "evLowChargeThreshold": {
        "title": "EV low charge alert threshold (%)",
        "type": "integer",
        "default": 20,
        "minimum": 1,
        "maximum": 80,
        "description": "HomeKit sends a low-battery notification when EV charge drops below this percentage. Default is 20%."
      },
      "tankCapacityLiters": {
        "title": "Fuel tank capacity (litres)",
        "type": "integer",
        "default": 70,
        "minimum": 10,
        "maximum": 150,
        "description": "Used to calculate fuel percentage. The XC90 2016 tank is 70 L — only change this if your variant differs."
      },
      "engineStartDuration": {
        "title": "Engine start duration (minutes)",
        "type": "integer",
        "default": 15,
        "minimum": 1,
        "maximum": 15,
        "description": "How long the engine runs when started remotely. Max 15 minutes (API limit)."
      },
      "pollInterval": {
        "title": "Poll interval (seconds)",
        "type": "integer",
        "default": 1800,
        "minimum": 10,
        "description": "How often the plugin fetches the latest vehicle state from Volvo. Default is 1800 (30 minutes) to stay well within the 10,000 requests/day API limit."
      },
      "showLock": {
        "title": "Show Lock",
        "type": "boolean",
        "default": true,
        "description": "Lock / unlock tile in the Home app."
      },
      "showClimate": {
        "title": "Show Climate Pre-condition",
        "type": "boolean",
        "default": true,
        "description": "Switch to start / stop climate pre-conditioning."
      },
      "showEngine": {
        "title": "Show Remote Start",
        "type": "boolean",
        "default": true,
        "description": "Switch to remotely start or stop the engine."
      },
      "showDoors": {
        "title": "Show Doors",
        "type": "boolean",
        "default": true,
        "description": "Contact sensors for all 6 openings (doors, hood, tailgate) with an at-a-glance summary tile."
      },
      "showFuel": {
        "title": "Show Fuel Level",
        "type": "boolean",
        "default": true,
        "description": "Fuel level as a percentage tile with a low-fuel alert below 15%."
      },
      "showCharging": {
        "title": "Show EV Battery",
        "type": "boolean",
        "default": true,
        "description": "EV battery charge level, charging state, and low-charge alert. Only meaningful on the T8 PHEV — disable on petrol-only variants."
      },
      "showWindows": {
        "title": "Show Windows",
        "type": "boolean",
        "default": true,
        "description": "Contact sensor tile showing open/closed state for all windows (front left, front right, rear left, rear right) and the sunroof. Summary tile shows Open if any window is ajar."
      },
      "showDiagnostics": {
        "title": "Show Diagnostics",
        "type": "boolean",
        "default": true,
        "description": "Contact sensor tile that alerts if any system warning is active: oil level, coolant level, brake fluid, washer fluid, service due, or tyre warnings. Tap the tile to see which specific system triggered."
      },
      "showHonkFlash": {
        "title": "Show Honk & Flash (combined)",
        "type": "boolean",
        "default": true,
        "description": "Momentary switch that honks the horn AND flashes the lights together. On by default. Disable if you prefer the separate controls below."
      },
      "showHonk": {
        "title": "Show Honk only",
        "type": "boolean",
        "default": false,
        "description": "Momentary switch that honks the horn only (no flash). Enable if your VIN supports the separate 'HONK' command — check the 'Supported commands:' line in the Homebridge log on startup."
      },
      "showFlash": {
        "title": "Show Flash only",
        "type": "boolean",
        "default": false,
        "description": "Momentary switch that flashes the lights only (no honk). Enable if your VIN supports the separate 'FLASH' command — check the 'Supported commands:' line in the Homebridge log on startup."
      },
      "debug": {
        "title": "Debug logging",
        "type": "boolean",
        "default": false,
        "description": "Log every API request, response, and state change to the Homebridge log. Useful for troubleshooting."
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "Always Required",
      "items": ["vccApiKey", "vin"]
    },
    {
      "type": "fieldset",
      "title": "First-time Setup",
      "description": "Fill in for initial authentication only. Once logged in, these can all be removed.",
      "expandable": true,
      "expanded": true,
      "items": ["username", "password", "otp", "forceReauth"]
    },
    {
      "type": "fieldset",
      "title": "Accessories",
      "description": "Choose which tiles appear in the Home app. All shown by default.",
      "expandable": true,
      "expanded": true,
      "items": ["showLock", "showClimate", "showEngine", "showHonkFlash", "showHonk", "showFlash", "showDoors", "showWindows", "showFuel", "showCharging", "showDiagnostics"]
    },
    {
      "type": "fieldset",
      "title": "Behaviour",
      "items": ["tankCapacityLiters", "evLowChargeThreshold", "engineStartDuration", "pollInterval"]
    },
    {
      "type": "fieldset",
      "title": "Advanced",
      "expandable": true,
      "expanded": false,
      "items": ["debug"]
    }
  ]
}

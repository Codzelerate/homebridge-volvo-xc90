{
  "pluginAlias": "VolvoXC90",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Control your Volvo XC90 2016 from Apple Home via the Volvo Connected Vehicle API v2. Get your VCC API Key at [developer.volvocars.com](https://developer.volvocars.com).\n\n**First-time setup:** Fill in all fields, save, and restart. Volvo will email you a 6-digit OTP — paste it into the OTP field, save, and restart again. Once authenticated, you can remove your email, password, and OTP from the config. Only the VCC API Key and VIN are needed going forward.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform name",
        "type": "string",
        "default": "Volvo XC90"
      },
      "vccApiKey": {
        "title": "VCC API Key",
        "type": "string",
        "required": true,
        "description": "Primary API key from your application on developer.volvocars.com. Always required."
      },
      "vin": {
        "title": "VIN",
        "type": "string",
        "minLength": 17,
        "maxLength": 17,
        "required": true,
        "description": "17-character Vehicle Identification Number. Always required."
      },
      "username": {
        "title": "Volvo ID (email)",
        "type": "string",
        "format": "email",
        "description": "Only needed for first-time setup or if the refresh token expires. Can be removed after successful login."
      },
      "password": {
        "title": "Volvo ID password",
        "type": "string",
        "description": "Only needed for first-time setup or if the refresh token expires. Can be removed after successful login."
      },
      "otp": {
        "title": "One-Time Password (OTP)",
        "type": "string",
        "description": "6-digit code from the Volvo email. Only needed during initial setup. Remove after successful login."
      },
      "forceReauth": {
        "title": "Force re-authentication",
        "type": "boolean",
        "default": false,
        "description": "Clears the stored session on next restart, triggering a fresh OTP login. Enable this + add your credentials, save and restart — you will receive a new OTP email. Disable again once authenticated."
      },
      "evLowChargeThreshold": {
        "title": "EV low charge alert threshold (%)",
        "type": "integer",
        "default": 20,
        "minimum": 1,
        "maximum": 80,
        "description": "HomeKit sends a low-battery notification when EV charge drops below this percentage. Default is 20%."
      },
      "tankCapacityLiters": {
        "title": "Fuel tank capacity (litres)",
        "type": "integer",
        "default": 70,
        "minimum": 10,
        "maximum": 150,
        "description": "Used to calculate fuel percentage. The XC90 2016 tank is 70 L — only change this if your variant differs."
      },
      "engineStartDuration": {
        "title": "Engine start duration (minutes)",
        "type": "integer",
        "default": 15,
        "minimum": 1,
        "maximum": 15,
        "description": "How long the engine runs when started remotely. Max 15 minutes (API limit)."
      },
      "serviceIntervalMonths": {
        "title": "Service interval (months)",
        "type": "integer",
        "default": 12,
        "minimum": 1,
        "maximum": 36,
        "description": "How many months between services. Used to calculate the Service Due % in the Diagnostics tile. Default is 12 (annual service)."
      },
      "serviceIntervalKm": {
        "title": "Service interval (km)",
        "type": "integer",
        "default": 30000,
        "minimum": 1000,
        "maximum": 100000,
        "description": "How many km between services. Used alongside the months interval — the lower % of the two is shown. Set to 0 to disable. Common Volvo intervals: 20,000 / 25,000 / 30,000 km."
      },
      "serviceAlertThreshold": {
        "title": "Service alert threshold (%)",
        "type": "integer",
        "default": 20,
        "minimum": 5,
        "maximum": 50,
        "description": "The Service Due sensor flips to Change Filter when service life drops below this %. Default 20% — roughly 2-3 months before a 12-month service is due."
      },
      "pollInterval": {
        "title": "Poll interval (seconds)",
        "type": "integer",
        "default": 1800,
        "minimum": 10,
        "description": "How often the plugin fetches the latest vehicle state from Volvo. Default is 1800 (30 minutes) to stay well within the 10,000 requests/day API limit."
      },
      "showLock": {
        "title": "Show Lock",
        "type": "boolean",
        "default": true,
        "description": "Lock / unlock tile in the Home app."
      },
      "showClimate": {
        "title": "Show Climate Pre-condition",
        "type": "boolean",
        "default": true,
        "description": "Switch to start / stop climate pre-conditioning."
      },
      "showEngine": {
        "title": "Show Remote Start",
        "type": "boolean",
        "default": true,
        "description": "Switch to remotely start or stop the engine."
      },
      "showDoors": {
        "title": "Show Doors",
        "type": "boolean",
        "default": true,
        "description": "Contact sensors for all 6 openings (doors, hood, tailgate) with an at-a-glance summary tile."
      },
      "showFuel": {
        "title": "Show Fuel Level",
        "type": "boolean",
        "default": true,
        "description": "Petrol tank percentage and estimated range (km to empty) inside the Energy tile."
      },
      "showCharging": {
        "title": "Show EV Battery",
        "type": "boolean",
        "default": true,
        "description": "EV battery charge level, charging state, and low-charge alert. Only meaningful on the T8 PHEV — disable on petrol-only variants."
      },
      "showWindows": {
        "title": "Show Windows",
        "type": "boolean",
        "default": true,
        "description": "Contact sensor tile showing open/closed state for all windows (front left, front right, rear left, rear right) and the sunroof. Summary tile shows Open if any window is ajar."
      },
      "showRange": {
        "title": "Show Range",
        "type": "boolean",
        "default": true,
        "description": "Show km-to-empty for EV battery and fuel tank. Display style is controlled by the Range view option below."
      },
      "rangeStandalone": {
        "title": "Range view",
        "type": "boolean",
        "default": true,
        "description": "On (default): EV Range and Tank Range appear as their own standalone tiles in the room view. Off: range values are shown as sub-sensors inside the Energy tile detail view."
      },
      "showLocation": {
        "title": "Show Car at Home sensor",
        "type": "boolean",
        "default": false,
        "description": "Adds an Occupancy Sensor that shows Occupied when the car is within the home radius, and Not Occupied when it is away. Requires homeLatitude and homeLongitude to be set."
      },
      "homeLatitude": {
        "title": "Home latitude",
        "type": "number",
        "description": "Latitude of your home in decimal degrees. Find it in Google Maps by right-clicking your home — the first number shown."
      },
      "homeLongitude": {
        "title": "Home longitude",
        "type": "number",
        "description": "Longitude of your home in decimal degrees. Find it in Google Maps by right-clicking your home — the second number shown."
      },
      "homeRadiusMeters": {
        "title": "Home radius (metres)",
        "type": "integer",
        "default": 200,
        "minimum": 50,
        "maximum": 2000,
        "description": "How close the car must be to count as home. Default 200m. Increase if the car parks on the street rather than your driveway."
      },
      "showDiagnostics": {
        "title": "Show Diagnostics",
        "type": "boolean",
        "default": true,
        "description": "Contact sensor tile that alerts if any system warning is active: oil level, coolant level, brake fluid, washer fluid, service due, or tyre warnings. Tap the tile to see which specific system triggered."
      },
      "showLeftOpen": {
        "title": "Show Left Open sensor",
        "type": "boolean",
        "default": false,
        "description": "Alerts when you lock the car but leave a door, window, sunroof, hood, or tailgate open. The sensor name updates dynamically to describe what was left open — e.g. 'Volvo: Door · Sunroof' — so the HomeKit notification tells you exactly what to go close. Enable notifications for this sensor in the Home app (tap the sensor → Settings → Notifications). Off by default."
      },
      "showHonkFlash": {
        "title": "Show Honk and Flash (combined)",
        "type": "boolean",
        "default": true,
        "description": "Momentary switch that honks the horn AND flashes the lights together. On by default. Disable if you prefer the separate controls below."
      },
      "showHonk": {
        "title": "Show Honk only",
        "type": "boolean",
        "default": false,
        "description": "Momentary switch that honks the horn only (no flash). Enable if your VIN supports the separate 'HONK' command — check the 'Supported commands:' line in the Homebridge log on startup."
      },
      "showFlash": {
        "title": "Show Flash only",
        "type": "boolean",
        "default": false,
        "description": "Momentary switch that flashes the lights only (no honk). Enable if your VIN supports the separate 'FLASH' command — check the 'Supported commands:' line in the Homebridge log on startup."
      },
      "showRefresh": {
        "title": "Show Refresh switch",
        "type": "boolean",
        "default": false,
        "description": "Adds a momentary 'Refresh' switch to the Controls tile. Flipping it triggers an immediate poll of all accessories — useful for checking the latest charger status, door state, etc. without waiting for the next scheduled poll."
      },
      "debug": {
        "title": "Debug logging",
        "type": "boolean",
        "default": false,
        "description": "Log every API request, response, and state change to the Homebridge log. Useful for troubleshooting."
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "Required",
      "description": "These two fields are always needed.",
      "items": ["vccApiKey", "vin"]
    },
    {
      "type": "fieldset",
      "title": "Authentication",
      "description": "For first-time login only. Fill in, save, restart, enter the OTP code Volvo emails you, then save and restart again. Remove email, password, and OTP once authenticated — only VCC API Key and VIN are needed going forward.",
      "expandable": true,
      "expanded": true,
      "items": ["username", "password", "otp", "forceReauth"]
    },
    {
      "type": "fieldset",
      "title": "Controls",
      "description": "Which switches appear inside the Volvo Controls tile.",
      "expandable": true,
      "expanded": false,
      "items": ["showLock", "showClimate", "showEngine", "showHonkFlash", "showHonk", "showFlash"]
    },
    {
      "type": "fieldset",
      "title": "Sensors",
      "description": "Which contact sensor tiles appear in the Home app.",
      "expandable": true,
      "expanded": false,
      "items": ["showDoors", "showWindows", "showLeftOpen"]
    },
    {
      "type": "fieldset",
      "title": "Diagnostics",
      "description": "Vehicle health panel: oil, coolant, brake fluid, washer fluid, service due, and all 4 tyres. This is the most API-intensive feature (4 endpoints per poll). Turn it off to roughly halve daily API usage if you don't need it.",
      "expandable": true,
      "expanded": false,
      "items": ["showDiagnostics"]
    },
    {
      "type": "fieldset",
      "title": "Energy",
      "description": "Fuel level, EV battery, and range sensors.",
      "expandable": true,
      "expanded": false,
      "items": ["showFuel", "showCharging", "showRange", "rangeStandalone"]
    },
    {
      "type": "fieldset",
      "title": "Location",
      "description": "Car at Home occupancy sensor. Requires your home coordinates.",
      "expandable": true,
      "expanded": false,
      "items": ["showLocation", "homeLatitude", "homeLongitude", "homeRadiusMeters"]
    },
    {
      "type": "fieldset",
      "title": "Vehicle",
      "description": "Specs specific to your car. Defaults match the XC90 2016 T8 PHEV.",
      "expandable": true,
      "expanded": false,
      "items": ["tankCapacityLiters", "engineStartDuration", "serviceIntervalMonths", "serviceIntervalKm"]
    },
    {
      "type": "fieldset",
      "title": "Alerts",
      "description": "Thresholds that trigger HomeKit notifications.",
      "expandable": true,
      "expanded": false,
      "items": ["evLowChargeThreshold", "serviceAlertThreshold"]
    },
    {
      "type": "fieldset",
      "title": "Advanced",
      "description": "Rarely needed. Change poll interval, enable the manual refresh switch, or turn on debug logging.",
      "expandable": true,
      "expanded": false,
      "items": ["pollInterval", "showRefresh", "debug"]
    }
  ]
}
